struct Error {
  message: str,
  code: i32,
}

function divide(a: f64, b: f64): f64 | Error {
  if (b == 0.0) {
    Error {
      message: "Division by zero",
      code: 1,
    }
  } else {
    a / b
  }
}
