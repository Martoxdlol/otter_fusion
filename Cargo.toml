[package]
name = "otter_fusion"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
insta = { version = "1.47.2", features = ["glob"] }
